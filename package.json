{
  "name": "style-chunk-collect",
  "version": "1.0.0",
  "description": "A repository for style chunk collect.",
  "main": "index.js",
  "scripts": {
    "dev": "lessc -v",
    "build": "npm run build-org && npm run build-minify",
    "build-org": "lessc src/main.less dist/index.css",
    "build-minify": "lessc src/main.less dist/index.min.css --clean-css=advanced",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "less": "^3.8.1",
    "less-plugin-clean-css": "^1.5.1"
  }
}
